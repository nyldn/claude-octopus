# Repository Toolkit

This is the harness-neutral controller for Claude Octopus. Use it with Claude
Code, Codex, Copilot, OpenCode, or any other coding agent.

## Start a Session

Read these sources in order before editing:

1. `AGENTS.md` for repository rules and the session-completion contract.
2. `AI_AGENT_HANDOFF.md` for the last verified state, decisions, blockers, and
   exact next action.
3. `git status --short --branch` and the latest commits on the active branch.
4. the relevant `bd` issue; `bd` is the task system of record.
5. the task-specific references linked below.

If Beads reports pending schema migrations, do not migrate the database. Record
the blockage and work in `AI_AGENT_HANDOFF.md`, then flag it to the maintainer.

## Repository Contracts

- Generated artifacts are never hand-edited. Run `make sync` after changing
  components, metadata, release notes, models, providers, or public facts.
  `make sync-check` must pass before commit.
- `scripts/sync-readme.py` owns current release, model roster, provider,
  capability, component, and test-suite facts across `README.md`,
  `.claude-plugin/README.md`, and `PRODUCT.md`.
- `.claude-plugin/marketplace.json` is generated by
  `scripts/sync-marketplace.sh`.
- Provider changes follow the complete checklist in `docs/PROVIDERS.md`.
  Provider case globs are order-sensitive.
- Releases follow `RELEASING.md`. Squash-merge the release PR, verify the exact
  merge commit on `main`, wait for that commit's required CI, and tag that
  commit rather than the branch head.
- Outbound GitHub comments and reviews never pass generated text or Markdown
  through inline shell body arguments. Stream the body to
  `./scripts/safe-gh-comment.sh --repo OWNER/REPO ... -` on stdin (or pass a
  private file); the helper snapshots, validates, and posts without echoing it.
- Scripts remain executable. Recheck for mode changes after local tests because
  test fixtures can change modes as a side effect.
- Run targeted tests while developing and `make ci-changed` before an ordinary
  branch push. Its checked-in manifest always runs sync, smoke, packaging, and
  reachability checks and fails closed to `make ci-local` for shared or unmapped
  changes. Run the complete `make ci-local` matrix before merge and release.
  At minimum, documentation-only changes run `make sync-check` and
  `git diff --check`.
- Preserve untracked harness-local files such as `.octo-continue.md` unless the
  user explicitly places them in scope.

## Finish a Session

1. Run the proportional quality gates and verify generated files are current.
2. File `bd` issues for every remaining item, then update or close the relevant
   issues. If writes are blocked, document that fact in the handoff without
   migrating.
3. Update `AI_AGENT_HANDOFF.md` with decisions, evidence, commit/push state,
   blockers, and the exact next action.
4. Commit only intended files, pull with rebase, push, and verify the branch is
   up to date with its remote.
5. Clean up stashes and prune merged or stale remote branches.
6. Check for unresolved review threads and open issues or pull requests before
   declaring the work complete.

## Task References

- Model ownership and escalation: `docs/MODEL-ROUTING-STRATEGY.md`
- Provider integration: `docs/PROVIDERS.md`
- Release process: `RELEASING.md`
- Architecture: `docs/ARCHITECTURE.md`
- Developer and verification guidance: `docs/DEVELOPER.md`
